version: '2'
services:
  mongodb:
    image: mongo
    networks:
      - server
    ports:
      - "1337:27017"

  juice-shop:
    build: ./
    depends_on:
      - mongodb
    networks:
      - server
    ports:
      - "3000:3000"

networks:
  server:
    driver: bridge
